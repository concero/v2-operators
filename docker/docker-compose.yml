version: "3.8"

services:
  app:
    container_name: relayer
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    env_file:
      - ./../.env
    volumes:
      - ../logs:/app/logs
    restart: always
