name: "relayers"

services:
  app:
    container_name: "relayer"
    ports:
      - "${RELAYER_PORT}:5000"
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    restart: unless-stopped
    volumes:
      - ../.deploy/logs:/app/logs
    env_file:
      - ../.env
    command: >
      sh -c "
      bun prisma migrate resolve --rolled-back 20260107105317_callbacks_count_default_value &&
      bun run prisma:migrate:deploy &&
      bun run start
      "
    depends_on:
      - postgres

  postgres:
    image: postgres:16.0
    environment:
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_DB: ${POSTGRES_DB}
    volumes:
      - ../.deploy/postgres:/var/lib/postgresql/data
    env_file:
      - ../.env
    container_name: ${DATABASE_HOST}
    hostname: ${DATABASE_HOST}
    ports:
      - ${DATABASE_PORT}:5432
    restart: unless-stopped
