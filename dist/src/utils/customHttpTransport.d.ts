import { Transport } from 'viem';
export declare function createCustomHttpTransport(url: string, config?: {
    timeout?: number;
    batch?: boolean;
    retryCount?: number;
    retryDelay?: number;
}): Transport;
//# sourceMappingURL=customHttpTransport.d.ts.map